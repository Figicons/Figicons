import Icon from './Icon';
import { preferences, setAttributes, setIcons } from './prefences';
import { IConfig } from './types/interfaces';

export { Icon, IConfig, preferences, setIcons, setAttributes };
